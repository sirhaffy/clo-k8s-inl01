# Secrets Configuration
namespace: "todo-app"

# Service Account for Workload Identity
serviceAccount:
  name: "default"

# Azure Key Vault integration
azure:
  tenantId: ""        # Set by GitHub Actions from Terraform output
  keyVaultName: ""    # Set by GitHub Actions from Terraform output
  keyVaultUri: ""     # Set by GitHub Actions from Terraform output

# Chart overrides
nameOverride: ""      # This values will be populated by the GitHub Actions workflow
fullnameOverride: ""  # This values will be populated by the GitHub Actions workflow